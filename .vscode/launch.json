{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Javascript Debugger",
      "processId": "${command:PickProcess}"
    },
    {
      "type": "node-terminal",
      "name": "Run Server",
      "request": "launch",
      "command": "npm run serve-test",
      "cwd": "${workspaceFolder}"
    }
  ]
}
